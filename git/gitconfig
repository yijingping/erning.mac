[user]
	name = yijingping
	email = jingping.yi@gmail.com
[alias]
    st = status
    s = status -sb
    df = diff --ignore-space-change
    ci = commit
    co = checkout
    rb = rebase
    br = branch
    ph = push
    pl = pull
    f = fetch
    r = remote
    rv = remote -v
    mg = merge
    dw = diff --word-diff
    lg = log -p 
[color]
    ui = auto
[core]
    autocrlf = input
    editor = vim
[credential]
	helper = cache --timeout=36000000
[push]
	default = simple
